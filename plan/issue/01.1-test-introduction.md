# #01.1 測試全面引入（backend 安全網，先於雲端遷移）

**Phase A — DB 層之後、Workers 化之前** ｜ 相依：#01（已實作 repo 層）｜ 後續：#02 起全程依賴

## 背景

目前**整個專案沒有任何自動化測試**（backend / frontend 皆無測試設定、無測試檔）。
而 #01 剛把 DB 層整個改寫成 Drizzle + repo 層，後續 #02（D1 driver）、#03（runtime 抽象）、
#04（R2）、#05（PR 輪詢）、#06（OG 上雲）每一步的核心驗收條件都是
**「self-host 行為零退化」**。沒有測試網，這個「零退化」只能靠人工點測，
每次雲端遷移都在裸奔。

本 issue 的定位是**在雲端遷移正式開始前，先把 backend 的安全網架好**：
建立 `bun test` 測試基礎建設、覆蓋純函式 / repo 層 / route 整合三層，
並接上 CI，讓之後 #02~#08 的每次改動都有迴歸保護。

## Decisions（主人已拍板，implementer 不需再問）

這些決定已定案，**不是開放問題，不要為此暫停等待人工輸入**：

1. **只做 backend**。frontend 測試留待 #08 或另開 issue。
2. **深度做到 route 整合**：純函式單元 + repo 層（隔離 SQLite）+ route（Hono `app.request()`）。
   外部依賴（GitHub / OpenAI / R2）一律 mock，不打真實網路。
3. **一併建立 CI**（GitHub Actions，push / PR 自動跑測試）。
4. **route 測試的 DB 隔離用 `DB_PATH` 暫存檔，本 issue 不做 db 注入式重構**；
   db 注入（`c.var.db`）與隨之而來的測試簡化**留給 #03**（在 #03 plan 補 follow-up）。
5. **coverage 只產報告、不當門檻**：CI 跑 `bun test --coverage` 並印出覆蓋率摘要供 review 參考，
   **不設 `coverageThreshold`、不因覆蓋率不足而擋 PR**。擋 PR 的只有「測試本身失敗」。
6. **測試目錄統一放 `backend/test/`**（含 `test/helpers/`），**不**使用 `src/**/__tests__`，避免兩處分裂。

## 需求摘要

- 為 backend 建立可重複執行、互不汙染、不依賴外部網路的 `bun test` 測試套件。
- 覆蓋三層：
  - **純函式單元**：`slugify`、`frontmatter` 等無 side effect 的 lib；`translator` 以 mock 處理（見下）。
  - **repo 層**：`repos/drafts`、`repos/presets`、`repos/images`，用隔離 SQLite 驗證 query 語意。
  - **route 整合**：透過 Hono `app.request()` 打 `/api/*`，驗證 status code / 回傳 JSON / DB 副作用。
- 外部依賴（GitHub REST、OpenAI、R2/S3）以 mock 隔離。
- 建立 GitHub Actions workflow，push 與 PR 時自動跑測試 + 印覆蓋率摘要（不擋）。
- 補上 `backend/package.json` 的 `test` / `test:coverage` script。

## 測試指令（明確定義，避免不一致）

| 用途 | 指令 | 行為 |
| --- | --- | --- |
| 本地快速跑（預設） | `bun test` | 跑全部測試，不算覆蓋率（最快） |
| 本地看覆蓋率 | `bun run test:coverage` | 跑測試 + 印覆蓋率摘要表（text reporter） |
| CI | `bun run test:coverage` | 同上；**測試失敗才紅燈**，覆蓋率僅作報告不擋 |

- `package.json` scripts：`"test": "bun test"`、
  `"test:coverage": "bun test --coverage --coverage-reporter=text"`。
- **覆蓋率報告靠 CLI flag 達成，不依賴 `bunfig.toml`**（避免被不確定的 key 卡住）。
- CI 一律呼叫 `bun run test:coverage`（單一入口，本地與 CI 行為一致）。

## 假設

- 沿用 Bun 內建測試器（`bun test`，`bun:test` 的 `describe/test/expect/mock`），
  不額外引入 jest / vitest（符合 backend「用 Bun 原生 API」原則）。Bun 版本 1.3.10。
- repo 層測試用 `makeTestDb()`（in-memory SQLite + 共用建表函式），不碰 production DB。
- route 整合測試靠**啟動前設定 `DB_PATH` 指向暫存檔**讓 db 單例落在隔離 DB 上（見「route 測試隔離」）。
- 測試只跑 self-host（bun-sqlite）路徑；D1 路徑的測試屬 #02 範圍。

## 非目標 / 不在範圍

- frontend 測試（Vitest / RTL / 純函式）—— 留待 #08 或另開 issue。
- D1 driver 路徑的測試 —— 屬 #02。
- 把 route 的 db 單例重構成注入式 —— 屬 #03（本 issue 只用 `DB_PATH` 隔離，不動 production 結構）。
- E2E（真實啟動 server + 真實 GitHub/R2）—— 不做，外部依賴一律 mock。
- 效能 / 壓力測試。
- **coverage 門檻 / gate** —— 明確不做，只產報告（見 Decisions #5）。

## 允許的 production 微調（僅此清單，需在 PR 標明）

本 issue 原則上**不改 production 邏輯**，唯一允許的微調是為了「測試與 production 共用單一來源」：

- **`applySchema(sqlite)` 抽離（建議做）**：把 `db.ts` 目前寫死在 top-level 的建表 DDL +
  ALTER migration 抽成一個**純函式**（無 side effect），`db.ts` 與 `makeTestDb()` 都呼叫它。
  目的：避免測試另抄一份 DDL 與 `db.ts` 漂移。抽離後 `db.ts` 的 side effect 順序需不變
  （建目錄 → `applySchema` → export db）。需更新該函式 JSDoc 的 `@remarks` 記錄為何抽離。
- **translator helper 抽離（不建議，預設不做）**：`translator.ts` 目前只 export
  `isTranslationEnabled` 與 `translateDraft`，**沒有**純解析 helper。本 issue **不為了測試而拆它**；
  改用 mock fetch + env 的方式測（見下）。除非主人另外指示，否則不動 `translator.ts`。

## Issue 細節

### Title

#01.1 backend 測試全面引入 + CI（純函式 / repo / route 三層 + GitHub Actions）

### Goal

backend 有一套可重複、隔離、離線的 `bun test` 測試套件，覆蓋純函式、repo 層、route 整合，
並由 CI 自動執行（覆蓋率僅報告）；成為 #02 之後所有雲端遷移的迴歸安全網。

### Scope（影響 / 新增檔案）

- `backend/package.json` — 新增 `"test"` 與 `"test:coverage"` script（見「測試指令」表）。
  覆蓋率報告由 `--coverage --coverage-reporter=text` CLI flag 達成，**不需要 `bunfig.toml`**。
- `backend/bunfig.toml`（**可選 enhancement，非必要**）— 若 implementer 想把 coverage 設定收進設定檔，
  可在 `[test]` 加報告選項；但**前提是先以 Bun 1.3.10 實測該 key 有效**，否則**直接略過此檔**，
  維持用 CLI flag。**一律不設 `coverageThreshold`**（不擋 PR）。本 issue 不因 bunfig 而卡關。
- 新增 `backend/test/`（所有測試檔與 helper 統一放這裡）：
  - `slugify.test.ts` — `slugify` / `isValidSlug` / `SLUG_PATTERN` 邊界（CJK→空字串、長度截斷、連字號收斂）。
  - `frontmatter.test.ts` — `parseFrontmatter` / `frontmatterToDraft` round-trip 與特殊字元。
  - `translator.test.ts` — `isTranslationEnabled` 依 env 的回傳；`translateDraft` 以 **mock `fetch`** 測
    （不打真實 OpenAI），env 在 import 受測模組前設好。
  - `repos/drafts.test.ts` — CRUD、`deleteDrafts` 批次、slug 衝突系列（`findSlugConflict` /
    `findSlugConflictExcludingIds` / `findSlugConflictBrief` / `findDraftsBySlug`）的 TRIM 語意、
    `listDrafts` 排序、`listSyncableDrafts` / `listPrOpenedDrafts` 過濾條件。
  - `repos/presets.test.ts` — presets CRUD。
  - `repos/images.test.ts` — list / upsert（同 key）/ insert。
  - `routes/drafts.test.ts` — `GET/POST /drafts`、`GET/PATCH/DELETE /drafts/:id`、batch publish/delete。
  - `routes/slug.test.ts`、`routes/presets.test.ts`、`routes/images.test.ts`、`routes/translate.test.ts`。
  - `routes/github.test.ts`、`routes/upload.test.ts`、`routes/og.test.ts` — **見下方「route 覆蓋範圍」**：
    這幾條依賴 GitHub / R2 / OG 生成，以 mock 隔離；若某條成本過高，可在本 issue **明確標記 deferred**
    並寫清楚理由，不可默默略過。
  - `backend/test/helpers/makeTestDb.ts` — 建立隔離 Drizzle 實例 + 呼叫 `applySchema()`，回傳 `db`（repo 測試用）。
  - `backend/test/helpers/setupRouteEnv.ts` — route 測試的 **bootstrap**：在任何 db 相關 import 前設好
    `process.env.DB_PATH`（固定 tmp 路徑）與外部依賴 mock，並以 dynamic import 載入 `testApp`（見「Test bootstrap contract」）。
  - `backend/test/helpers/testApp.ts` — 從 `src/routes/api.ts` 組一個測試用 Hono app（見「route 測試隔離」）。
  - `backend/test/helpers/mocks.ts` — GitHub / OpenAI / R2 的 `mock.module()` 替身。
- 新增 `.github/workflows/test.yml` — push / PR 時 `oven-sh/setup-bun@v2` → `bun install --frozen-lockfile`
  → `bun run test:coverage`（`working-directory: backend`）。**測試失敗才紅燈**；覆蓋率只印不擋。

### Test bootstrap contract（route 測試的硬規則，務必逐條照做）

這是 route 整合測試**能否正確隔離**的核心。背景事實（已核實）：

- `src/lib/db.ts` 在 **module load 當下**就讀 `process.env.DB_PATH`、`mkdir`、開檔、建表（top-level side effect）。
- ESM **static import 會在測試檔 body 執行之前先解析並執行**。因此「在測試檔頂端寫 `process.env.DB_PATH = ...`
  再 `import { api } from ...`」**不可靠**——static import 早就先跑了 `db.ts`，env 設定來不及生效。
- `backend/index.ts` top-level 直接呼叫 `startPRChecker()`，且 default export 是 `{ port, fetch }`（非 Hono 實例）。

**硬規則（route 測試一律遵守）：**

1. **禁止 static import 任何會間接 import `src/lib/db.ts` 的模組**（含 `src/routes/api.ts`、各 route、
   `testApp.ts`、`makeTestDb.ts`、repo 模組）。route 測試檔頂端**只能** static import `bun:test` 與
   `setupRouteEnv` 這類「不碰 db」的 helper。
2. **載入順序固定為**：先設 `process.env.DB_PATH`（固定 tmp 路徑）→ 註冊外部依賴 `mock.module()`
   → 再用 **dynamic `await import("...")`** 載入 `testApp` / `api.ts` / repo。把這整段封裝進
   `backend/test/helpers/setupRouteEnv.ts`，route 測試檔呼叫它取得已就緒的 `app`。
3. **`testApp` 組 app 方式**：`new Hono().route("/api", api)`，`api` 來自 `src/routes/api.ts`（dynamic import）。
   只掛 router、**不 import `index.ts`**、不觸發 `startPRChecker()`。
4. **替代方案（亦可）**：用 Bun 的 **preload**（`bunfig.toml` 的 `test.preload` 或 `bun test --preload`）
   指向一支 setup 檔，在所有測試檔載入前先設好 `DB_PATH` 與 mock；此時 route 測試可正常 static import。
   **二擇一即可**，但若用 preload 仍須確保 preload 檔先於 db.ts 載入；**預設採規則 1–3 的 dynamic import**，
   因為它不依賴 bunfig（與 coverage 決策一致：不被 bunfig key 卡住）。

**共用 DB_PATH 來源（消除模糊）：**

- 由 `setupRouteEnv.ts` **匯出一個固定常數** `ROUTE_TEST_DB_PATH = join(os.tmpdir(), "blog-editor-route-test.db")`
  並在內部 `process.env.DB_PATH = ROUTE_TEST_DB_PATH`。**所有 route 測試檔都經由它**，保證看到同一個值、
  同一個 db 單例（因為 db.ts 單例在 process 內只建一次）。route 測試檔**不得各自硬寫 DB_PATH**。

**清表 / 隔離（具體範圍）：**

- 採「共用暫存 DB + `beforeEach` 清空」。`setupRouteEnv` 提供 `resetDb()`，在每個 route 測試的
  `beforeEach` 呼叫，**清空全部三張表**：`drafts`、`translation_presets`、`images`
  （`DELETE FROM` 各表）。避免任何 route 測試之間互相污染。
- `afterAll` 刪除 `ROUTE_TEST_DB_PATH` 檔，確保不在 repo 留下 `data/`。

### route 覆蓋範圍（明確列舉，避免遺漏）

- **完整測**（純本地邏輯 + DB）：`drafts`、`slug`、`presets`、`images`（R2 部分 mock）。
- **mock 外部後測**：`translate`（mock OpenAI fetch）、`github`（mock `lib/github`）。
- **`upload` / `og`**：依賴 R2 與 OG 生成。本 issue **只測「參數驗證 / 錯誤路徑」**
  （缺參數、未啟用、無效輸入回正確 status/錯誤訊息），**happy-path 明確 deferred 到後續 issue**
  （在 PR 與本檔註明），不在本 issue 現場判斷成本。

### Dependencies

- #01（Drizzle + repo 層）已完成 —— 本 issue 直接針對 repo 函數與 route 撰寫測試。

### Suggested implementation order

1. 加 `test` / `test:coverage` script + 建 `backend/test/` 目錄。
2. **純函式測試先行**（slugify / frontmatter）—— 最快見綠燈，建立信心。
3. 抽 `applySchema()` + `makeTestDb` helper + repo 層測試 —— 確立 DDL 單一來源這個地基。
4. `setupRouteEnv` + `testApp` helper + route 整合測試（照「Test bootstrap contract」：先設 env/mock
   再 dynamic import；`beforeEach` 清空三表；translator mock fetch）。
5. GitHub Actions workflow（跑 `test:coverage`），開 PR 驗證綠燈與覆蓋率摘要有印出。
6. 補 README 測試章節（如何跑、如何加測試、覆蓋率只是報告）。

### Acceptance criteria

**本地**
- [ ] `cd backend && bun test` 本地全綠，且**可重複執行不互相汙染**（不殘留 `data/` 真實 DB 痕跡）。
- [ ] `bun run test:coverage` 能跑完並印出覆蓋率摘要表。

**測試覆蓋**
- [ ] 三層皆有測試：純函式（slugify/frontmatter/translator）、repo（drafts/presets/images）、
      route 整合（drafts/slug/presets/images/translate/github；upload/og 至少測錯誤路徑或標 deferred）。
- [ ] 至少涵蓋 #01 改寫過的高風險點：slug 衝突 TRIM 語意、batch delete/publish、
      `listDrafts` 排序、`listSyncableDrafts` 過濾。

**隔離**
- [ ] 測試**不依賴外部網路**：GitHub / OpenAI / R2 全部 mock，斷網也能跑過。
- [ ] repo 測試用 `makeTestDb()` 隔離 SQLite，**不碰** production `data/blog-editor.db`。
- [ ] route 測試遵守「Test bootstrap contract」：先設 `DB_PATH`/mock 再 **dynamic import**、經 `setupRouteEnv`
      共用同一 tmp DB、不 static import 碰 db 的模組、不 import `index.ts`、不啟動 `startPRChecker()`。
- [ ] route 測試 `beforeEach` 清空 `drafts`/`translation_presets`/`images` 三表，`afterAll` 刪除暫存 DB 檔。

**CI / coverage**
- [ ] `.github/workflows/test.yml` 在 push / PR 觸發，跑 `bun run test:coverage`。
- [ ] CI 在**測試失敗時**紅燈；覆蓋率**僅印摘要、不設門檻、不因覆蓋率擋 PR**。
- [ ] `backend/package.json` 有 `test` 與 `test:coverage`（`bun test --coverage --coverage-reporter=text`）script；
      **無 `coverageThreshold` 設定**（不論用不用 `bunfig.toml`）。

### Testing / verification strategy

- **純函式**：直接 import 呼叫，table-driven 測邊界（空字串、CJK、特殊字元、長度上限）。
- **translator**：`isTranslationEnabled` 切換 env 驗證；`translateDraft` 用 `mock.module()` 或覆寫
  `globalThis.fetch` 攔截 OpenAI 呼叫，env 於 import 前設定。
- **repo 層**：`makeTestDb()` 建乾淨 db → seed → 呼叫 repo 函數 → 斷言回傳與 DB 狀態；`beforeEach` 重建/清空。
- **route 整合**：經 `setupRouteEnv`（先設 env/mock → dynamic import）取得 `app`，
  `app.request("/api/...", { method, body })` → 斷言 `res.status`、`await res.json()`、以及 DB 副作用。
  外部呼叫用 `mock.module()` 攔截 `lib/github`、`lib/translator`、`lib/r2`。
- **CI**：`bun install --frozen-lockfile` + `bun run test:coverage`；測試失敗即擋，覆蓋率僅報告。

### Risks and mitigations

- **建表 DDL 重複（最大風險）**：DDL 寫死在 `db.ts` top-level。→ 抽 `applySchema(sqlite)` 純函式，
  `db.ts` 與 `makeTestDb` 共用單一來源（見「允許的 production 微調」）。
- **ESM static import 早於 env 設定（最易踩雷）**：static import 在測試檔 body 前就執行，會在
  `DB_PATH` 設定前先載入 `db.ts`、鎖定錯誤 DB（甚至 production `data/blog-editor.db`）。
  → 「Test bootstrap contract」硬規則：route 測試先設 env/mock，再 **dynamic `await import`**；
  禁止 static import 任何碰 db 的模組。
- **`db.ts` import 即有 side effect（`mkdir`/開檔/建表）**：→ 經 `setupRouteEnv` 在 dynamic import 前
  設好 `DB_PATH`（`os.tmpdir()` 暫存檔），`afterAll` 刪除。
- **誤 import `index.ts` 啟動 `startPRChecker()`**：→ 一律用 `testApp` 從 `src/routes/api.ts`（dynamic import）組 app，
  禁止 import `index.ts`。
- **跨檔共用 db 單例造成資料殘留**：→ 經 `setupRouteEnv` 共用同一 `ROUTE_TEST_DB_PATH`，
  `beforeEach` 清空 `drafts`/`translation_presets`/`images` 三表。
- **coverage 設定卡關**：→ 覆蓋率用 CLI flag（`--coverage --coverage-reporter=text`）達成，
  `bunfig.toml` 為可選；不設門檻、不因覆蓋率失敗。若採 bunfig 須先實測 key 在 Bun 1.3.10 有效，否則略過。
- **mock 漂移**：mock 介面與真實 `lib/github` 等簽章不一致會假綠。→ mock 緊貼簽章、用 `typeof` 對齊。
- **D1 無關**：本 issue 不測 D1；#02 換 driver 後另補（標記給 #02）。

### Notes for the implementer

- **先讀後改**：抽 `applySchema` 前 grep 所有 import `db.ts` 的地方，確認 side effect 順序不變。
- 測試一律放 `backend/test/`，不要用 `src/**/__tests__`。
- 外部依賴 mock 用 `bun:test` 的 `mock.module()`，放在測試檔頂端、import 受測模組之前。
- repo 測試的建表（`applySchema`）務必含 #01 migration 欄位（`github_path`/`github_sha`/`slug`），否則 slug 系列測試會炸。
- route 測試務必照「Test bootstrap contract」：先設 `DB_PATH`/mock → **dynamic import** → 用 `testApp`，
  絕不 static import 碰 db 的模組、絕不 import `index.ts`。
- 依專案規範：為可測性微調函式（如抽 `applySchema`）要同步更新 JSDoc，`@remarks` 記錄 why。
- CI 用 `oven-sh/setup-bun@v2`、`working-directory: backend`、cache `~/.bun/install/cache`。

## 依賴與排序備註

- 本 issue 應在 **#02 之前**完成並合併 —— 安全網要先於它要保護的改動存在。
- **#03 follow-up（已定案，請寫進 #03 plan）**：#03 完成 db 注入（`c.var.db`）後，把 route 整合測試
  從「`DB_PATH` 暫存檔隔離」升級為「測試 db 直接注入 app」，移除對 `DB_PATH` 與 module 載入順序的依賴。

## 風險審查（彙整）

| 風險 | 嚴重度 | 緩解 |
| --- | --- | --- |
| ESM static import 早於 env 設定，鎖錯 DB | 高 | bootstrap contract：先設 env/mock 再 dynamic import |
| 建表 DDL 在測試與 `db.ts` 漂移 | 高 | 抽 `applySchema(sqlite)` 純函式單一來源 |
| `db.ts` import side effect 汙染 | 中 | 經 `setupRouteEnv` 在 dynamic import 前設 `DB_PATH`、`afterAll` 清理 |
| 誤啟動 `startPRChecker()` | 中 | 用 `testApp` 組 app，禁 import `index.ts` |
| 跨檔 db 單例殘留 | 中 | 共用 `ROUTE_TEST_DB_PATH` + `beforeEach` 清空三表 |
| mock 與真實介面漂移 | 中 | mock 緊貼簽章、`typeof` 對齊 |
| coverage 設定卡關 | 低 | 用 CLI flag，bunfig 可選、不設門檻 |
