services:
  blog-editor:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - blog-editor-data:/data
    env_file:
      - backend/.env

volumes:
  blog-editor-data:
